# Models (optional)

Plants are generated procedurally at runtime from the plant profile; nothing here is
required. This folder is reserved for optional baked assets (for example a GLB pot or a
leaf atlas exported from Blender). Blender is a build-time tool only, never a runtime
dependency, and the tooling to export from it is not set up yet (Blender was not
available when the renderer was built).
