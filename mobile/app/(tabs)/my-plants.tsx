import { Placeholder } from '@/components/Placeholder';

// TODO(ui-owner): port web/src/components/MyPlants.tsx (collection + watering countdown; AsyncStorage instead of localStorage).
export default function MyPlantsScreen() {
  return <Placeholder title="My plants" note="Your collection and watering reminders." />;
}
