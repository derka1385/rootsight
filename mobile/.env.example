# Optional. By default the app calls the API on the machine running `expo start`, port 8787.
# Set this to point at another server (e.g. a deployed API). It is bundled into the app, so never put secrets here.
# EXPO_PUBLIC_API_URL=http://192.168.1.12:8787
